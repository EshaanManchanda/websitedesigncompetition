# Production Environment Variables Example
# Copy this file to .env.production when deploying to production
# cp .env.production.example .env.production

# API Configuration - Use clean subdomain pattern without /api prefix
# Nginx will add the /api prefix when proxying to backend
VITE_API_URL=https://api.websitedesigningcompetition.com
